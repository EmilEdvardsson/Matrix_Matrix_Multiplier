Usage: mpirun -n X matmul input.txt output.txt
Where X is number of processes
input.txt is a whitespace delimited document containing values of
matrix A and matrix B, in row major format.
Output is the resulting matrix C in row major format.